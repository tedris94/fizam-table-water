# Skip "reinstall node_modules from scratch?" prompt (pnpm 9+). Safe for dev/CI.
confirm-modules-purge=false

# Payload SQLite on Vercel: native libsql must be hoisted for serverless tracing.
public-hoist-pattern[]=libsql
public-hoist-pattern[]=@libsql/*
shamefully-hoist=true
