/** Stored on orders when the customer chooses factory pickup (must match Paystack init). */
export const PICKUP_ORDER_ADDRESS =
  'Pickup — customer collection at FIZAM facility (details sent after payment).'
